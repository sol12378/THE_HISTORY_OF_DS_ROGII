[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rogii"
version = "0.1.0"
description = "ROGII Wellbore Geology Prediction competition workspace"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

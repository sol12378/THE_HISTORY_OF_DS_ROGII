# 2026-05-25

## Plan

- Obsidianのプロジェクト記憶ルールをagent指示へ追加する。
- `ROGII_kaggle_obsidian/` のvault構成を作る。
- ROGII向けのデータエンジニアリング方針を文書化する。
- base table生成、fold生成、validationを実装する。

## Do

- `AGENTS.md` を更新した。
- `CLAUDE.md` を更新した。
- `agent.md` の互換入口を追加した。
- Obsidian directories、index notes、templates、feature notes、CV notes、submission notes、data engineering notesを作成した。
- versioned base tables、feature registry、metadata sidecars、validation gates、migration policyを含む長期データ設計を強化した。
- target masks、GR missingness、TVT deltas、naive baselines、fold balanceに注目したEDAを行った。
- `scripts/build_base_tables.py` でbase tableを生成できるようにした。
- `scripts/make_folds.py` でtarget row数を均等化したwell単位foldを生成できるようにした。
- `scripts/validate_data.py` でbase table、submission id、TVT_input tail、fold fileを検証できるようにした。
- `source_path` は公開時に個人パスが混ざらないよう `data/raw/...` 形式の相対パスにした。
- 公開リポ安全ルールを `AGENTS.md`、`agent.md`、`CLAUDE.md` に追加した。
- `scripts/run_baseline_anchor.py` でanchor baselineを実行した。
- `scripts/run_baseline_lgbm.py` でLightGBM delta baselineを実行した。
- macOS上のLightGBM実行に必要な `libomp` をHomebrewで導入した。
- Kaggle CLIでローカルCSV提出を試したが、Notebook-only competitionのため拒否された。
- Kaggle Notebook `sol12378/rogii-exp003-lgb-anchor-trajectory` を作成し、version 3で `submission.csv` を生成した。
- `kaggle competitions submit ... -k sol12378/rogii-exp003-lgb-anchor-trajectory -v 3` でNotebook出力を提出した。
- `scripts/analyze_oof.py` を実装し、`exp004_oof_error_slicing` としてwell別・hidden_length別・trajectory形状別のOOF分析を実行した。

## Check

- Obsidianに `10_Data_Engineering/` sectionができた。
- agent instructionsで、hypotheses、changes、experiment results、decisions、next actionsのObsidian更新を必須化した。
- data architectureはstable base tablesとmutable feature versionsを分離する設計になった。
- Anchor baselineは全train target rowsで約15.91 RMSEと強い。
- 素朴なslope extrapolationは不安定で、未正則化のまま使うべきではない。
- GroupKFold by wellはtarget-row countsをバランスさせるべき。
- base table validationは成功。
- train horizontal wells: 773、test horizontal wells: 3。
- sample submission id と test target id は 14,151件で完全一致。
- `TVT_input` 欠損tail違反はtrain/testともに0。
- fold target rowsは5foldで約756k-758kに揃った。
- `exp001_anchor_baseline` のoverall CV RMSEは 15.909853。
- `exp003_lgb_anchor_trajectory` のoverall CV RMSEは 15.054865。
- LightGBMはanchorからRMSEを 0.854988 改善した。
- 行単位では 53.2% のtarget rows、well単位では 446 / 773 wells でLightGBMがanchorより改善した。
- LightGBMはbiasを -1.596 から -0.109 へ改善したが、一部wellでは大きく悪化している。
- Kaggle submission一覧で `SubmissionStatus.COMPLETE` と Public LB 14.147 を確認した。
- `exp004_oof_error_slicing` で、LightGBMがanchorより改善したwellは446 / 773、悪化したwellは327 / 773だった。
- RMSEがanchorより10以上悪化したwellは15個あり、worstは `727a3a10` で model RMSE 50.205、anchor RMSE 12.364。
- trajectory形状別では `downward_curved` だけがshape単位でanchorより悪化した。

## Act

- 次は `Z` 系特徴量のablationと、well別error slicingを行う。
- `09_Submissions/LB_Tracking.md` にpublic scoreとCV-LB gapを追記した。
- 次は `downward_curved` とworst wellsに対して、deltaを弱めるblendまたはshape別補正を試す。
- `train_base_v001.parquet` / `test_base_v001.parquet` をstable artifactsとして使う。
- pushはユーザーの明示許可があるまで行わない。
